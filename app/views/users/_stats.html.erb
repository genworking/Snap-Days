<% @user ||= current_user %>
<div class="stats">
  <div class="follow-info">
    <div class="followers">
      <a href="<%#= followers_user_path(@user) %>">フォロワー</a>
      <a id="followers" class="stat"><%= @user.followers.count %></a>
    </div>
    <div class="following">
      <a href="<%#= following_user_path(@user) %>">フォロー</a>
      <a id="following" class="stat"><%= @user.followings.count %></a>
    </div>
  </div>
</div>
