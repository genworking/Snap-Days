<% if favorite_list.present? %>
  <p>お気に入り登録済み</p>
  <% favorite_list.each do |post| %>
    <%= link_to(post_path(post)) do %>
      <%= image_tag post.photos.first.image.url(:medium), class: "favorite-list-img" %>
    <% end %>
  <% end %>
<% else %>
  <p>お気に入り登録はまだありません</p>
<% end %>
