<!DOCTYPE html>
<html>
  <head>
    <%= render 'layouts/haed' %>
  </head>
  <body>
    <div id="wrapper">
      <%= render 'partial/navbar' if current_user %>
      <%= render 'partial/flash' %>
      <div class="container">
        <%= yield %>
      </div>
      <%= render 'partial/footer' %>
    </div>
  </body>
</html>
